{
  "name": "notes",
  "version": "1.0.0",
  "description": "",
  "main": "./backend/index.js",
  "scripts": {
    "installBackend": "npm i --prefix ./backend",
    "installFrontend": "npm i --prefix ./frontend && npm run build --prefix ./frontend",
    "start": "npm run start --prefix backend",
    "dev": "npx nodemon backend/index.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run installBackend && npm run installFrontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
